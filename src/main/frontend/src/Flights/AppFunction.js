import React, { useState } from "react";
//import Flight from "../Flights/Flight.js";
//import FlightList from "../Flights/FlightList.js";

/*
export default function AppFunction() {
  const [Flight, setNewFlight] = useState({});
  const handleChange = ({ target }) => {
    const { name, value } = target;
    setAllFlights({});
    setNewFlight({});//change to Post call
  };

  const [allFlights, setAllFlights] = useState([]);
  const handleSubmit = (event) => {
    event.preventDefault();
    if (!flight.flightNum) return;
    setAllFlights((prev) => [newFlight, ...prev]);//post call
    setNewFlight({});
  };


  return (
    <main>
      <h1>Flights</h1>
      <Flight
        newFlight={newFlight}
        handleChange={handleChange}
        handleSubmit={handleSubmit}
      />
      <FlightList allFlights={allFlights} />
    </main>
  );
}
*/
